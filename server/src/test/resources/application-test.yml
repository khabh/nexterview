spring:
  sql:
    init:
      mode: never
  datasource:
    url: jdbc:mysql://localhost:3306/nexterview_test?useSSL=false&serverTimezone=UTC
  jpa:
    defer-datasource-initialization: false
    hibernate:
      ddl-auto: create-drop
